[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "MenegerLLMs"
version = "0.1.0"
dependencies = [
    "requests>=2.34.2"
]

[tool.setuptools.packages.find]
where = ["."]